spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:file:~/dev
    username: sa
    password:
    driver-class-name: org.h2.Driver
    #data: populate_h2.sql

server:
  servlet:
    context-path: /refapp
  port: 8001